# 项目规则

## 注释
- 注释块不超过两行；单行注释过长时折叠为两行
- 代码部分始终使用中文注释，避免英文注释，包括 Rust 以外的文件，如脚本文件和标记语言文件

## 代码质量（每次编辑后检查）
- 检查代码中是否有可重构、可优化的代码块，并进行处理；优先合并可合并的代码，合并后复查是否还能进一步删除
- 检查代码是否已格式化：Rust 一律使用 rustfmt 默认规则（`cargo fmt --manifest-path Project\Cargo.toml`），提交前运行 `cargo fmt -- --check` 确认无 diff
- 清理未使用的 use 导入和包
- 确保包都是最新的稳定版本，版本号精确到第三位，而不是前两位
- 编译验证：`cargo clippy --release --manifest-path Project\Cargo.toml` 必须 0 警告 0 错误

## 会话整理
- 每次对话完整整理本文件和 CHANGELOG.md，并进行格式化使 Markdown 看起来规整（标点符号、错别字、润色）
- 项目代码部分有重要变动时在 CHANGELOG.md 中记录（本文件只保留项目规则）
- 每次推送代码到仓库时，提交消息使用简短的英文总结，且不能出现版本号
- 建立新版本 release 资产时，格式须与往期版本一致，并使用英文
- 两个 README 必须保持同步，其中的表格必须对齐格式化
- 重大修改和调优前记得先备份，防止翻车；确认正常后清除备份
